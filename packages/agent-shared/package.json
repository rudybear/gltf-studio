{
  "name": "@gltf-studio/agent-shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared proposal pipeline (CommandChain, validateProposalGraph, refusal-error base classes) both AgentService providers (agent-mock, agent-llm) build on — specs/agent-service.md, docs/adr/0004, docs/adr/0005.",
  "license": "MIT",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b .",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run"
  },
  "dependencies": {
    "@gltf-studio/editor-core": "workspace:*",
    "@gltf-studio/engine-api": "workspace:*",
    "@gltfi/ir": "0.0.1",
    "@gltfi/verify": "0.0.1"
  },
  "devDependencies": {
    "@gltfi/gltf": "0.0.1",
    "vitest": "^2.1.8"
  }
}
