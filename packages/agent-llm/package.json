{
  "name": "@gltf-studio/agent-llm",
  "version": "0.0.1",
  "private": true,
  "description": "OpenAICompatibleAgentProvider: an AgentService implementation against a local OpenAI-compatible /v1/chat/completions tool-calling endpoint (Ollama, LM Studio) — specs/agent-service.md AG-017..AG-022, docs/adr/0005-local-openai-compatible-llm-provider.md. Resolves the response's tool_calls (never text) into Commands via the shared @gltf-studio/agent-shared pipeline (CommandChain, validateProposalGraph) built on editor-core's GraphEdit/SceneEdit factories.",
  "license": "MIT",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b .",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run"
  },
  "dependencies": {
    "@gltf-studio/agent-shared": "workspace:*",
    "@gltf-studio/editor-core": "workspace:*",
    "@gltf-studio/engine-api": "workspace:*"
  },
  "devDependencies": {
    "@gltf-studio/contract-tests": "workspace:*",
    "@gltfi/gltf": "0.0.1",
    "vitest": "^2.1.8"
  }
}
